[phases.setup]
nixPkgs = ["nodejs-18"]

[phases.install]
cmds = [
    "rm -rf node_modules/.cache 2>/dev/null || true",
    "npm ci"
]

[phases.build]
cmds = ["echo 'Build complete'"]

[phases.start]
cmd = "npm start"