{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf node_modules/.cache 2>/dev/null || true && npm ci"
  },
  "deploy": {
    "postdeploy": "npm run migrate"
  }
}